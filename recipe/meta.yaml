{% set name = "xonda" %}
{% set version = "0.2.2" %}
{% set sha256 = "0276e82b992e0b41297a4856281d53f9a7d3066f8f224a7600b9a15bdaab5efa" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/gforsyth/{{ name }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: python setup.py install

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - xonsh
    - conda

test:
  requires:
    - pytest
  source_files:
    - tests/
    - xontrib/
  commands:
    - py.test -v .

about:
  home: https://github.com/gforsyth/xonda
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: 'This is a thin wrapper around conda for use with xonsh'

  doc_url: https://github.com/gforsyth/xonda
  dev_url: https://github.com/gforsyth/xonda

extra:
  recipe-maintainers:
    - CJ-Wright
    - gforsyth
